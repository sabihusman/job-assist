# Job Assist API base URL.
#
# Local dev: http://localhost:8000 (run `uv run uvicorn job_assist.main:app`
#            from apps/api/).
# Vercel:    https://job-assist-api.up.railway.app  (set this in the Vercel
#            project's environment variables for Preview + Production).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
